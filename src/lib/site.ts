/* Canonical site origin — single source of truth for metadata, sitemap,
   robots, and JSON-LD. Update here (and nowhere else) when the domain changes.
   No trailing slash. */
export const SITE_URL = "https://kaungkhantlin-portfolio-website.vercel.app";
